import {StyleSheet} from 'react-native';

const myStyles = theme =>
  StyleSheet.create({
    mainContainer: {
      flex: 1,
    },
    contentContainer: {
      flexGrow: 1,
    },
  });

export default myStyles;
